# values.yaml — valores por defecto

replicaCount: 1

image:
  repository: nginx
  #por ajsute de version  
  tag: "1.27.4"       # <-- aquí cambiamos la versión
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
